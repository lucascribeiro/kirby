#ifndef KIRBY_H
#define KIRBY_H
#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>

class kirby
{
    public:
        kirby();
        virtual ~kirby();

    protected:

    private:
};

#endif // KIRBY_H
